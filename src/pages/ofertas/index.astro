---

import GlobalLayouts from '../../layouts/GlobalLayouts.astro';
import  { dataOffer } from '../../components/data/offferData'
import ShowAllBanner from '../../components/ShowAllBanner.astro';

const posts = dataOffer.sort(
	(a, b) => a.pubDate.valueOf() - b.pubDate.valueOf()
);

---

		<GlobalLayouts title="Phone Store">
	  <ShowAllBanner posts={posts} path="ofertas" />

		</GlobalLayouts>
		
		

